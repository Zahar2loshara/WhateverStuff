A (initially python) script that generates like 6k lines of commands for the datapack;
and since it needed to have at least a one interger range, i decided to add a binary into it cuz why tf not, want some ciphers in my stuff
